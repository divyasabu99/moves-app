kind = "web"
previewPath = "/pitch/"
title = "MOVES Pitch Teleprompter"
version = "1.0.0"
id = "artifacts/pitch"
router = "path"

[[services]]
name = "web"
paths = [ "/pitch/" ]
localPort = 20938

[services.development]
run = "pnpm --filter @workspace/pitch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pitch", "run", "build" ]
serve = "static"
publicDir = "artifacts/pitch/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20938"
BASE_PATH = "/pitch/"
